name: nirnay_field_module
description: "Centralized Mobile Field Inspection & CCTV Surveillance Module for DoSJE (Smart India Hackathon)"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  web: ^0.5.1
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
